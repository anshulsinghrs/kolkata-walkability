# Kolkata Walkability Atlas

> Street-level **Perceived Walkability Scores (PWS)** for the city of Kolkata,
> plus a fully interactive geospatial analytics platform for uploading,
> reweighting, and exporting urban observation datasets.

A static web application built as part of the
**Integrated Active Mobility Safety Matrix (IAMSM)** PhD research at IIT
Kharagpur.

## Interactive features (frontend, no backend required)

- **CSV / GeoJSON / KML upload** — drag-and-drop or browse. PapaParse streams
  large files and the UI surfaces a progress bar plus per-column detection
  (lat / lon / weightage / category / hazard / timestamp / notes / image_url
  / indicator columns).
- **Three visualisation modes** for uploaded data:
  - **Points** — canvas-rendered, gradient-coloured by weightage
  - **Heatmap** — Leaflet.heat density surface, weighted by score
  - **Clusters** — Leaflet.markercluster with mean-score bubbles
- **Live indicator-weight recompute** — sliders for sidewalk / greenery /
  lighting / crowdedness / crossing-safety; weights are L1-normalised and
  applied to any uploaded rows that carry those columns.
- **Five basemaps** — Dark, CartoDB Positron, OpenStreetMap Standard,
  Esri Satellite, OpenTopoMap.
- **Point detail drawer** — click any point for coordinates, score,
  hazard / category / timestamp, indicator breakdown, notes, and (when
  provided) an uploaded image preview.
- **Exports** — GeoJSON (visible or all-filtered), CSV (visible), and a
  PNG snapshot of the map composite.

A sample dataset for the upload flow ships at
`frontend/data/sample-upload.csv`.

---

## Repository structure

```
.
├── frontend/                  # Static site — this folder is what gets deployed
│   ├── index.html             # Entry HTML
│   ├── css/style.css          # All styling (dark academic theme)
│   ├── js/                    # Modular vanilla JavaScript
│   │   ├── config.js          # Constants (colors, zoom, paths)
│   │   ├── color-scale.js     # PWS → RGB mapping
│   │   ├── data-loader.js     # Manifest + viewport-based tile fetching
│   │   ├── points-layer.js    # Custom Leaflet canvas overlay
│   │   ├── map-view.js        # Leaflet map + basemaps
│   │   ├── ui-controls.js     # Sliders, toggles, status pill
│   │   └── app.js             # Orchestration
│   └── data/                  # Generated by backend/process_data.py
│       ├── manifest.json
│       ├── overview.json
│       └── tiles/*.json
│
├── backend/                   # Offline data-processing pipeline (Python)
│   ├── process_data.py        # CSV → spatial tiles + manifest
│   ├── requirements.txt
│   └── README.md              # Pipeline documentation
│
├── docs/
│   ├── ARCHITECTURE.md        # How the system fits together
│   └── DEPLOYMENT.md          # Step-by-step GitHub Pages setup
│
├── .github/workflows/
│   └── deploy.yml             # Auto-deploys frontend/ to GitHub Pages
│
├── .gitignore
└── README.md                  # You are here
```

---

## Architecture in one paragraph

GitHub Pages serves only static files, so there is no live API. Instead, the
**backend** is a one-shot Python script that converts a raw PWS CSV into a
manifest plus a grid of small JSON "tiles". The **frontend** loads the
manifest, renders a sampled overview at low zoom, and lazy-fetches only the
tiles intersecting the current viewport at high zoom — keeping memory and
bandwidth low even for 400,000-point datasets.

See `docs/ARCHITECTURE.md` for the long version.

---

## Quick start

### 1. Prepare the data (one-time, or whenever the dataset updates)

```bash
cd backend
pip install -r requirements.txt
python process_data.py --input /path/to/aggregated_pixel_ratios.csv
```

This writes `manifest.json`, `overview.json`, and a `tiles/` directory into
`frontend/data/`.

### 2. Test locally

```bash
cd frontend
python -m http.server 8000
```

Open <http://localhost:8000>.

### 3. Deploy to GitHub Pages

1. Push the repository to GitHub (create the repo if needed).
2. In the GitHub UI: **Settings → Pages → Build and deployment → Source:
   GitHub Actions**.
3. Push to `main`. The workflow in `.github/workflows/deploy.yml` builds and
   deploys automatically. The site will be available at
   `https://<your-username>.github.io/<repo-name>/`.

For the full deployment walkthrough — including troubleshooting and custom
domain configuration — see `docs/DEPLOYMENT.md`.

---

## What's on the map

| Element | Meaning |
|---------|---------|
| Coloured dot | One sampling point along Kolkata's pedestrian network |
| Red → Green | PWS gradient: very poor → excellent walkability (0–100) |
| Click a point (zoom ≥ 14) | Inspect exact PWS, lat/lon, and rating |
| Score slider | Filter to show only points within a PWS range |
| Basemap toggles | Dark / Light / Satellite |

---

## Methodology (brief)

The Perceived Walkability Score per point combines four built-environment
indicators extracted from a Google Street View panorama via the **Mask2Former**
semantic segmentation model:

1. **Sidewalk** — share of pixels classified as walkable surface
2. **Greenery** — vegetation pixel ratio
3. **Streetlight** — presence of street lighting infrastructure
4. **Crowdedness** — density of pedestrians and vehicles

Indicator weights come from a **binary-logit stated-preference model**
calibrated on a survey conducted as part of the PhD research. The output is
normalised to a 0–100 scale where higher is more walkable.

Full methodology is documented in the registration report (not in this repo;
contact the author).

---

#

---

## Author

**Anshul Singh**
PhD Researcher, [Ranbir & Chitra Gupta School of Infrastructure Design and
Management](https://www.iitkgp.ac.in/department/IF), IIT Kharagpur


---

## License

Code: MIT.
Data: research output — please cite the underlying PhD work if used.
